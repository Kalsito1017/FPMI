## Summary

<!-- Briefly describe what this PR does and why. -->

## Related Issue

<!-- Link to the issue this PR addresses, e.g. "Closes #123". -->

## Type of change

- [ ] Bug fix
- [ ] New feature
- [ ] Breaking change
- [ ] Documentation

## Checklist

- [ ] Tests pass (`npm test` in both `backend/` and `frontend/`)
- [ ] Lint passes (`npm run lint` in both `backend/` and `frontend/`)
- [ ] Typecheck passes (`npm run build` in both `backend/` and `frontend/`)
- [ ] Build passes (`npm run build` in both `backend/` and `frontend/`)
- [ ] Docs updated (e.g. `docs/api-contract.md`) if behaviour changed
- [ ] Commits follow Conventional Commits
